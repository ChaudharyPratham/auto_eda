# ─── Web framework ────────────────────────────────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ─── Data processing ──────────────────────────────────────────────────────────
pandas==2.2.1
numpy==1.26.4
scipy==1.12.0

# ─── File format support ──────────────────────────────────────────────────────
openpyxl==3.1.2        # Excel (.xlsx)
pyarrow==15.0.2        # Parquet + fast CSV
fastparquet==2024.2.0  # Alternative Parquet engine
lxml==5.2.1            # HTML table parsing (required for .ipynb notebook outputs)
fastavro>=1.9.0        # Apache Avro file support

# ─── Large-file processing (optional – comment out if Java is unavailable) ────
pyspark==3.5.1

# ─── Image processing ────────────────────────────────────────────────────────
Pillow>=10.0.0

# ─── Database ─────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0  # PostgreSQL driver

# ─── Cloud storage (all optional – only used when env vars are set) ───────────
azure-storage-blob>=12.19.0
boto3>=1.34.0
google-cloud-storage>=2.14.0
databricks-sdk>=0.20.0

# ─── Fuzzy value matching (combine mode staging layer) ────────────────────────
rapidFuzz>=3.6.0

# ─── Utilities ────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
aiofiles==23.2.1

# ─── Streaming Analytics (Kafka producer lib + Postgres driver) ───────────────
kafka-python==2.0.2
psycopg2-binary>=2.9.0  # already listed above; kept here for clarity

